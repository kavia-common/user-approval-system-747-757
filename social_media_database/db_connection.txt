This project uses a SQLite file database.

Environment variable used by backend:
- SQLITE_DB=./myapp.db (see .env.example)

To inspect using sqlite3:
  sqlite3 ./myapp.db

Ensure the file exists or initialize via project-specific initialization scripts if provided.
